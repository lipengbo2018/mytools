# 工具站导航需求文档

## 项目定位

`mytools.html` 是一个个人工具站导航页面，用于集中收藏和管理日常使用的工具网站。用户可以按分类筛选工具、收藏常用工具、新增工具站、维护分类，并导入/导出自己的工具配置。

## 核心目标

- 快速查看已配置的工具站。
- 在首页和收藏菜单中按分类筛选、按关键词搜索工具站。
- 收藏常用工具站，并在收藏菜单集中展示。
- 支持用户自行新增、编辑和删除工具站。
- 支持拖拽工具站卡片调整展示顺序。
- 支持用户维护工具分类。
- 支持工具站列表和分类配置的导入、导出。
- 支持从默认配置文件初始化工具站数据。
- 点击工具站后，在新网页中打开对应 URL。

## 菜单结构

页面包含2个主菜单：

1. 首页
2. 收藏

### 首页

首页展示工具站列表，并提供分类筛选能力。

功能要求：

- 默认展示全部工具站。
- 展示已有分类，用户选择某个分类后，只展示该分类下的工具站。
- 分类筛选以平铺按钮展示，不使用下拉框。
- 提供“全部”和“未分类”筛选项，“未分类”放在分类列表最后。
- 分类筛选和搜索框放在同一行，搜索框较短，支持模糊搜索。
- 每个工具站卡片展示工具名称、所属分类和收藏状态，默认不展示 URL。
- 每个工具站卡片提供收藏/取消收藏入口。
- 支持拖拽工具站卡片调整首页展示顺序，排序结果保存到浏览器本地配置。
- 单击工具站卡片或打开按钮，在新网页中跳转到该工具站 URL。
- 页面初始状态没有工具和分类，不展示额外空状态文案。

### 收藏

收藏菜单展示用户收藏过的工具站。

功能要求：

- 只展示已收藏的工具站。
- 收藏菜单同样提供平铺分类筛选和搜索框，并放在同一行。
- 用户可以在收藏菜单里取消收藏。
- 取消收藏后，该工具站应立即从收藏列表中移除。
- 收藏菜单中的工具站也支持拖拽排序，排序结果同步影响同一套工具站数据。
- 单击工具站卡片或打开按钮，在新网页中跳转到该工具站 URL。
- 没有收藏工具站时，不展示额外空状态文案。

## 工具站数据

每个工具站至少包含以下字段：

```json
{
  "id": "唯一标识",
  "name": "工具站名称",
  "url": "工具站 URL",
  "categoryIds": ["所属分类 ID"],
  "isFavorite": false
}
```

字段说明：

- `id`：工具站唯一标识，用于收藏、编辑和删除。
- `name`：工具站名称，用于展示和搜索。
- `url`：工具站地址，按用户输入原样保存，不自动添加协议前缀；卡片中默认隐藏。
- `categoryIds`：工具站所属分类列表。一个工具站可以属于多个分类，没有分类时为空数组。
- `isFavorite`：是否收藏。

## 分类数据

每个分类至少包含以下字段：

```json
{
  "id": "唯一标识",
  "name": "分类名称"
}
```

字段说明：

- `id`：分类唯一标识。
- `name`：分类名称，用于首页筛选和工具站归类。

## 新增工具站

用户可以新增工具站。

功能要求：

- 提供新增工具站入口。
- 新增时至少填写工具站名称和 URL。
- 新增时可以选择一个或多个所属分类；没有分类时允许保存为未分类。
- URL 按用户输入原样保存，允许本地链接、自定义协议或普通网页链接。
- 新增成功后，工具站出现在首页。
- 如果选择了某个分类，工具站也应出现在对应分类筛选结果中。

## 修改工具站

用户可以修改已有工具站。

功能要求：

- 支持修改工具站名称。
- 支持修改工具站 URL。
- 支持修改工具站所属分类，一个工具站可以同时属于多个分类。
- 修改后首页和收藏菜单中的数据应同步更新。

## 修改分类

用户可以维护分类。

功能要求：

- 支持新增分类。
- 支持修改分类名称。
- 支持删除分类。
- 删除分类前应提示用户该分类下是否存在工具站。
- 删除含有工具站的分类后，从相关工具站的分类列表中移除该分类；如果工具站不再属于任何分类，则归为未分类。

## 收藏逻辑

收藏状态在首页和收藏菜单之间保持一致。

功能要求：

- 在首页收藏某个工具站后，该工具站应出现在收藏菜单。
- 在首页取消收藏后，该工具站应从收藏菜单移除。
- 在收藏菜单中取消收藏后，首页里的收藏状态也应同步更新。
- 首页和收藏菜单中的分类筛选、搜索结果应基于同一套工具站数据。

## 导入导出

用户可以导入和导出工具站列表与分类配置。

### 导出

功能要求：

- 导出内容包含工具站列表、分类列表和收藏状态。
- 导出格式使用 JSON。
- 导出的 JSON 可作为备份，也可在其他浏览器或设备中导入。
- 导出文件名格式为 `mytools.default-YYYY-MM-DD.json`，其中日期为导出当天日期，例如 `mytools.default-2026-06-11.json`。

导出数据结构：

```json
{
  "version": 1,
  "categories": [],
  "tools": []
}
```

### 导入

功能要求：

- 支持导入 JSON 配置文件。
- 导入前校验数据格式。
- 导入内容包含分类、工具站和收藏状态。
- 导入时需要处理重复数据。
- 导入成功后，首页和收藏菜单立即刷新。

重复数据处理：

- 如果工具站 URL 已存在，导入数据覆盖名称、分类，并合并收藏状态。
- 如果分类名称已存在，复用已有分类。

## 默认配置

项目包含3个与工具站页面相关的文件：

- `mytools.html`：工具站导航页面。
- `mytools.default.json`：默认工具站配置。
- `mytools_readme.md`：需求文档。

页面启动时的数据加载顺序：

1. 优先读取当前浏览器 `localStorage` 中的用户配置。
2. 如果本地没有配置，则加载同目录下的 `mytools.default.json`。
3. 默认配置加载成功后，会写入当前浏览器 `localStorage`。
4. 后续用户在页面中的修改只影响当前浏览器本地配置，不会自动改写 `mytools.default.json`。

更新默认配置的推荐流程：

1. 在页面中新增、编辑分类和工具站。
2. 确认配置正确后，点击“导出配置”。
3. 用导出的 `mytools.default-YYYY-MM-DD.json` 内容覆盖仓库中的 `mytools.default.json`。
4. 提交并推送到 GitHub。
5. GitHub Pages 更新后，新设备或清空本地数据的浏览器会使用新的默认配置。

## 页面交互

- 工具站卡片应清晰展示名称、所属分类和收藏状态，默认隐藏 URL。
- 单击工具站卡片或打开按钮，应使用新网页打开用户保存的 URL。
- 收藏按钮、编辑按钮、删除按钮应避免触发跳转。
- 拖拽工具站卡片可调整排序；在筛选、收藏或搜索结果中拖拽时，只重排当前可见工具，其他工具保持原有相对顺序。
- 首页和收藏菜单都提供平铺分类筛选和搜索，二者放在同一行；未分类放在最后。
- 新增、编辑、删除、导入、导出等操作应有明确成功或失败提示。
- 所有数据变更后，当前页面视图应立即更新。
- 初始无数据时，页面不展示空状态文案。

## 数据存储

默认配置使用 `mytools.default.json` 随仓库永久保存；用户运行时配置使用浏览器本地存储保存。

存储内容：

- 工具站列表
- 分类列表
- 收藏状态
- 工具站排序

后续如果需要账号级多设备实时同步，可以再扩展为后端存储或云端同步。

## 非目标范围

当前阶段不要求实现具体工具功能，例如文本转语音、图片压缩、二维码生成等。

本项目只负责工具站导航、分类、收藏和配置管理。
